#ifndef __DLL_INJECTOR_LOG__
#define __DLL_INJECTOR_LOG__

#include <stdio.h>

// this function displays the input buffer in stdin in a classic hexdump format
void hexdump(unsigned char * buffer,int bufferSize);

#endif